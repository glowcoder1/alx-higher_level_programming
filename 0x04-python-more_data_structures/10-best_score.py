#!/usr/bin/python3
def best_score(a_dictionary):
    res = None
    max = -9999
    if a_dictionary:
        for (key, value) in a_dictionary.items():
            if value > max
            max = value
            res = key
    return res
